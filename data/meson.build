install_data('pixmaps/asunder.png',
  install_dir: join_paths(get_option('datadir'), 'pixmaps'),
)

install_data('asunder.desktop',
  install_dir: join_paths(get_option('datadir'), 'applications'),
)
